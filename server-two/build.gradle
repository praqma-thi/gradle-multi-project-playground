task 'build' {
    group 'Demo'
    description "Builds ${project.name}"

    doLast {
        println "[${target}] Built ${project.name}"
    }
}

task 'startServer' {
    group 'Demo'
    description 'Starts a server on port 9090'

    doLast {
        println "Started server on port 9090"
    }
}
