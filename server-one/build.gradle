apply from: "${rootProject.projectDir}/shared/server-tasks.gradle"

task 'build' {
    group 'Demo'
    description "Builds ${project.name}"

    doLast {
        println "[${target}] Built ${project.name}"
    }
}
