task 'info' {
    doLast {
        println """\
            Hello, I am the ${project.name} info task.
            You called me with gradle version ${gradle.gradleVersion}.
            I am NOT using a deprecated task notation.
            """.stripIndent()
    }
}
